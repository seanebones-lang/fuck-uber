// JS shim for native regex extract
// price: parseFloat(text.match(/\$(\d+(?:\.\d+)?)/)?.[1] || '0')
// miles: parseFloat(text.match(/(\d+(?:\.\d+)?)\s*mi/)?.[1] || 1)
// shared: text.includes('Shared')
// stops: parseInt(text.match(/(\d+)\s*stops?/)?.[1] || '1')