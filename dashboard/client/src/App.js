import React, {useState, useEffect} from 'react'; import {LineChart, Line, XAxis, YAxis, CartesianGrid, Tooltip} from 'recharts'; function App() { const [statsHistory, setStatsHistory] = useState([]); const [stats, setStats] = useState({accepts:0,rejects:0,uptime:99.95}); useEffect(() => { const ws = new WebSocket('ws://localhost:3000/stats'); ws.onmessage = e => { const data = JSON.parse(e.data); setStats(data); setStatsHistory(prev => [...prev.slice(-19), data]); }; return () => ws.close(); }, []); return (<div><h1>Mystro Dashboard</h1><p>Accepts: {stats.accepts} | Rejects: {stats.rejects} | Uptime: {stats.uptime}%</p><LineChart width={600} height={300} data={statsHistory}><CartesianGrid/><XAxis dataKey="time"/><YAxis/><Tooltip/><Line type="monotone" dataKey="accepts" stroke="#82ca9d"/><Line type="monotone" dataKey="rejects" stroke="#ff7300"/></LineChart></div>); } export default App;