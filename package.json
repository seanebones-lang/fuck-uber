{
  "name": "fuck-uber",
  "version": "1.0.0",
  "description": "Fuck Uber - Flawless Destro Clones for all platforms/agents",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter \"*\" run dev",
    "build": "pnpm --filter \"*\" run build",
    "test": "node ./node_modules/vitest/vitest.mjs run shared/",
    "test:dashboard": "cd dashboard && npm test -- --watchAll=false",
    "lint": "pnpm --filter \"*\" run lint",
    "benchmark": "node shared/benchmark.js"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "pnpm": "^10.13.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.3",
    "@vitest/coverage-v8": "^2.1.3",
    "ws": "^8.18.0"
  },
  "packageManager": "pnpm@10.13.0",
  "engines": {
    "node": ">=20.0.0"
  }
}